import React from 'react'

const Button = ({title}) => {
  return (
    <button style={{backgroundColor:'#407ec9',width:'150px',height:'30px',color:'white'}} >{title}</button>
  )
}

export default Button